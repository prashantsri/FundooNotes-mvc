@model System.Collections.Generic.List<tblLabel>
@using FundooNotesData.Data.Models;
@using Newtonsoft.Json;

<html>
<head>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
    <script src="http://ajax.googleapis.com/ajax/libs/jquery/1.7.1/jquery.min.js" type="text/javascript"></script>
</head>
<body ng-controller="notectrl">
    <div id="myModal" class="modal">
        <div class="modal-content">
            <md-dialog aria-label="Add Label">
                <md-card style="padding-bottom:20px;padding-top:20px">
                    <div style="font-weight:bold">Edit Label</div>
                    <div layout="row">
                        @*<form name="formValidation">

                            <div layout="column" layout-align="start center" flex>
                                <md-input-container md-no-float="true">
                                    <input placeholder="create New Label" name="addLabel" ng-model="label" required />
                                    <div ng-messages="formValidation.addLabel.$error">
                                        <div ng-message="required">Label is required.</div>
                                    </div>

                                </md-input-container>
                            </div>
                            <div layout="row" layout-align="end center" style="margin-top:-40px" flex>
                                <md-button ng-disabled="formValidation.addLabel.$invalid" ng-click="Label()">
                                    <md-icon md-svg-icon="~/Images/Done.svg"></md-icon>
                                </md-button>
                            </div>
                        </form>*@
                        <form name="formValidation">
                            <div layout="row">


                                <div layout-align="start center" flex>
                                    <md-input-container md-no-float="true">
                                        <input placeholder="Create New Label" name="addLabel" ng-model="label" required style="  width: 124px;" />
                                        <div ng-messages="formValidation.addLabel.$error">
                                            <div ng-message="required">Label is required.</div>
                                        </div>

                                    </md-input-container>
                                </div>
                                <div layout-align="end center" style="margin-top:-40px" flex>
                                    <md-button ng-disabled="formValidation.addLabel.$invalid" ng-click="Label()" style=" margin-top: 63px;">
                                        <md-icon md-svg-icon="~/Images/Done.svg"></md-icon>
                                    </md-button>
                                </div>
                            </div>
                        </form>

                    </div>

                    <div layout="column">
                        @if (Model != null)
                        {
                            foreach (tblLabel obj in Model.Reverse<tblLabel>())
                            {
                                <span style="margin-left:60px;padding-bottom:20px" ng-model="SetLabel">@obj.Label</span>

                            }


                        }
                    </div>

                </md-card>


            </md-dialog>
        </div>
    </div>

</body>


</html>
